//save back-end url in a variable to call it latter in API
const baseUrl = 'http://localhost:3000'

// Function that makes possible to make a REQ [GET] to the API
const getVagas = () => {
        // FETCH = used to comunicate via REQ 
}